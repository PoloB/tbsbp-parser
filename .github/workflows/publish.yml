name: Publish

on:
  release:
    types: [published]

jobs:

  # ── Full CI ───────────────────────────────────────────────────────────────────
  ci:
    name: "ci"
    uses: ./.github/workflows/ci.yml
    secrets: inherit   # forwards all secrets

  # ── Build ─────────────────────────────────────────────────────────────────────
  build:
    name: "build"
    runs-on: ubuntu-latest
    needs: [ci]

    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0   # full history required for hatch-vcs to compute version

      - uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0

      - name: Build sdist and wheel
        run: uv build

      - name: Check dist contents
        run: uvx twine check dist/*

      - name: Upload dist artifacts
        uses: actions/upload-artifact@v7
        with:
          name: dist
          path: dist/
          retention-days: 7

  # ── Publish to PyPI (releases only) ──────────────────────────────────────────
  publish-pypi:
    name: "publish / pypi"
    runs-on: ubuntu-latest
    needs: [build]
    environment: pypi
    if: github.event_name == 'release'

    permissions:
      id-token: write

    steps:
      - name: Download dist artifacts
        uses: actions/download-artifact@v8
        with:
          name: dist
          path: dist/

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1

  # ── Attach artifacts to GitHub Release ───────────────────────────────────────
  attach-artifacts:
    name: "attach artifacts"
    runs-on: ubuntu-latest
    needs: [publish-pypi]
    if: github.event_name == 'release'

    permissions:
      contents: write

    steps:
      - name: Download dist artifacts
        uses: actions/download-artifact@v8
        with:
          name: dist
          path: dist/

      - name: Attach sdist and wheel to GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          files: dist/*